# Copy this file to .env and fill in the values.
# Never commit .env to git.

# ── Supabase ──────────────────────────────────────────────────────────────────
# Shared project across all publications for this client.
# Same SUPABASE_URL and SUPABASE_ANON_KEY used by the public site and ops panel.
# Get from: Supabase dashboard → Project Settings → API
SUPABASE_URL=
SUPABASE_ANON_KEY=

# ── Netlify ───────────────────────────────────────────────────────────────────
# Token: app.netlify.com/user/applications → Personal access tokens
# Site ID: Netlify site → Site configuration → Site ID
NETLIFY_AUTH_TOKEN=
NETLIFY_SITE_ID=

# ── Server-side secrets ───────────────────────────────────────────────────────
# These do NOT go here. Set them in the Netlify dashboard:
# Netlify site → Site configuration → Environment variables
#
# SUPABASE_SERVICE_ROLE_KEY   (needed for user management — never in frontend)
# RESEND_API_KEY              (Phase 2 — newsletter sending)
